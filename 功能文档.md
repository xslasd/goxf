# goxf 微服务开发框架核心功能手册

```text
                      __ 
                     / _|
   __ _   ___ __  __| |_ 
  / _' | / _ \\ \/ /|  _|
 | (_| || (_) |>  < | |  
  \__, | \___//_/\_\|_|  
   __/ |                 
  |___/
```

`goxf` 是一个基于 Go 语言的高性能、企业级微服务开发框架。它秉承着**高内聚、低耦合**的设计理念，将微服务开发生命周期中常用的基础组件进行模块化组装，开箱即用，极大地提高了业务侧的开发效率。

---

## 1. 核心运行容器 (`application`, `goxf.go`)

- **服务编排体系**：提供全局统一的 `Service` 容器对业务环境进行托管。支持通过 `goxf.NewService()` 链式组装与启动各个下游协议组件。
- **声明式分层生命周期**：
  - **`InitBase()` (基础环境初始化)**：加载指定配置文件（支持 `-c` 参数）、初始化 `application.Runtime` 运行时元数据、初始化统一日志系统。不打印启动 Banner、不连接外部 ETCD 注册中心或 Jaeger Tracer。
  - **`Bootstrap()` (全量微服务初始化)**：在 `InitBase` 基础上完成 Banner 打印、Jaeger Tracer 链路追踪注册以及 ETCD 服务注册。
  - **`Run(servers...)` (服务监听与信号管理)**：按序启动各个 Server 并监听操作系统退出信号（SIGQUIT, SIGTERM, SIGINT），实现平滑优雅停机。
- **智能 CLI 子命令分发拦截**：
  - 在 `NewService()` 阶段自动识别命令行输入。若命中了注册的扩展子命令（如 `migrate`, `scaffold`, `version`），框架会自动直接执行该子命令逻辑并退出，**绝不产生多余的微服务网络启动开销**。
- **生命周期 Hook 机制**：内建基于闭包回调的四阶段 Hook（`Stage_BeforeLoadConfig`、`Stage_BeforeRun`、`Stage_BeforeStop`、`Stage_AfterStop`），允许用户在服务启停阶段自由注入拦截、清理或初始化逻辑。

---

## 2. 工程化配置管理 (`conf`)

- **多实例与 Functional Options 隔离**：
  - **全局单例加载 (`conf.LoadFromSource`)**：从指定数据源读取配置并**深度合并（Deep Merge）**至全局 `defaultConfiguration` 中，供全局 `conf.GetString` / `conf.UnmarshalKey` 直接读取。
  - **独立隔离实例 (`conf.NewConfFromSource` / `conf.NewFileConfSource`)**：创建一个全新的、完全独立的 `*conf.Conf` 实例，拥有专属的 Key 分隔符、独立配置数据与私有密码，不污染全局单例。
  - **丰富灵活的 Option 配置选项**：
    - `conf.WithWatch(bool)`：配置是否动态监听配置变更；
    - `conf.WithUnmarshal(Unmarshal)`：指定自定义解包函数（支持注入自定义 YAML/JSON/TOML 处理器）；
    - `conf.WithKeyDelimiter(string)`：自定义嵌套 Key 访问分隔符（如 `.`、`::` 等）；
    - `conf.WithPassword(pwd, cryptFn...)`：为该配置源指定独立的加解密密码与散列处理函数；
    - `conf.WithIgnoreGlobalPassword()`：忽略全局密码配置，保持明文读取，避免多实例密码干扰。
- **配置本地化覆盖（`.local` 机制）**：
  - 当框架加载主配置文件（如 `config.yaml` 或 `config.toml`）时，会自动嗅探同级目录下是否存在带有 `.local` 后缀的私有配置文件（如 `config.local.yaml`）。
  - 若存在，框架会自动读取并执行**深度合并（Merge）**，优先使用本地配置项覆盖主配置。这非常有利于团队协作：开发者可以在本地创建 `.local` 文件来调试数据库和测试环境，而不必担心误提交污染 Git 仓库的主配置。
- **多配置加密/解密防覆盖与任意 `.enc` 支持**：
  - **多配置独立命名**：
    - `config.yaml` / `config.local.yaml` $\rightarrow$ 加密生成 `system.enc`；解密还原为 `config.yaml`；
    - `custom.yaml` / `custom.local.yaml` $\rightarrow$ 加密生成 `custom.enc`；解密还原为 `custom.yaml`；
    - 彻底避免在多数据源场景下加解密目标文件相互覆盖的问题。
  - **任意 `.enc` 加密文件识别**：框架能够自动识别任何以 `.enc` 结尾的加密配置文件并进行透明解密。
  - **明确的路径验证提示**：交互式密码输入与报错会明确显示正在校验的配置文件路径（如 `Enter config password for [./examples/config.yaml]: `）。

---

## 3. 高性能服务组网 (`server`, `client`)

- **多协议标准化 Server 设计**：抽象出一套无侵入的 `server.Server` 标准，包含预留 HTTP（`sgin/gin`）、RPC、WebSocket、SSE（Server-Sent Events，提供与 WebSocket 极简风格高度一致的 Hub/Client 双向/单向通信封装）以及可能延展的自定义私有协议，实现统一管控并行执行。
- **业务中间件自动注入**：针对 HTTP 网关，集成标准且全面的跨域 (`CORS`) 及其鉴权(`Auth/JWT`) 生命周期守护网卡收敛。
- **健壮的外部 Client 生态**：内置原生级别的常用存储介质交互适配器：
  - `credis`：标准 Redis 分布式交互客户端。
  - `cetcd`：处理分布式配置、节点锁、注册订阅的 Etcd 工具集。
  - `carangodb`：图数据库 ArangoDB 交互等。

---

## 4. 强大的统一日志台 (`log`)

- **无极性能拓展**：底层深度封装了 Uber `zap` 标准引擎。
- **异步日志与归档切分**：可通过配置轻松实现文件日志落盘缓存刷新（可异步 `bws` 处理高并发），与标准自动切割归档策略（大小、时间）。
- **组件日志隔离适配**：能无缝适配/劫持 gRPC、XORM 引擎、Jaeger 等框架的私有日志接口并将收敛标准统一到 goxf 内。

---

## 5. 多维度服务治理扩展

- **JWT / 签名与鉴权体系 (`auth`)**：实现企业级权限安全签名管控 `JWTCertifier` 及令牌全生命周期处理机制。
- **微服务可观测性分析：**
    - **`metric` (Prometheus 等)**：无缝嵌入指标暴露器分析应用健康态。
    - **`tracer` (Jaeger 链路追踪)**：为分布式进程网提供标准、轻量的 `OpenTracing` 链路信息。
- **强类型后台任务引擎 (`job`)**：提供生产级别、高度抽象的后台任务托管方案：
    - **`job/cron` (定时任务)**：基于配置化调度的标准 Cron 任务引擎，支持秒级调度。
    - **`job/queue` (异步队列 & 延迟任务)**：内置通用的泛型句柄驱动（`Worker[T]`），实现消息的强类型分发与无缝断言。
        - **底层 Broker 驱动抽象**：提供 `MemoryBroker` (纯内容极管、基于时间轮优化的高性能 O(1) 协程分配) 及 `credis.QueueBroker` (持久化的高稳定 Redis 分发机制)。
        - **延迟任务 (Delayed Jobs)**：提供开箱即用的延迟投递功能 `EnqueueAfter`。在 Redis 下通过 ZSET+Lua 高可靠保障无丢失，利用极简接口优雅应对“订单超时判定/延迟回调”场景。
    - 均已标准收归框架生命周期守护，默认注入所有 Panic 恢复体系与底层 Prometheus 监控（`ServerHandleHistogram` / `ClientHandleHistogram`）。
- **微服务状态监控 (`registry`)**：预留灵活规范的服务注册与发现（Registry / Selector），以极速拓展在 Service Mesh 或纯微服务内环境通信的协调操作。

---

## 6. 标准化基础构建块

- **现代化 CLI 与多命令体系 (`flag`)**：基于社区工业级事实标准 `github.com/spf13/cobra` 与 `pflag` 深度定制：
  - **POSIX 标准与短参数**：原生支持长短参数（如 `-c` / `--config`、`-w` / `--watch`、`-v` / `--version`、`-h` / `--help`）。
  - **三级配置优先级回退**：优先级严格遵循 `命令行显式参数 > 环境变量 (EnvVar) > 默认值 (Default)`。
  - **结构体自动映射 (`BindStruct`)**：支持通过结构体 Tag（如 `flag:"config,c" env:"GOXF_CONFIG" default:"config.yaml" usage:"..."`）一键反射绑定参数。
  - **按需基础环境子命令注册**：
    - **`flag.AddCommand(cmds...)`**：注册普通离线/工具类子命令（如 `version`, `scaffold` 代码脚手架等，0 依赖、秒级启动）；
    - **`flag.AddCommandWithBase(cmds...)`**（别名 `flag.AddBaseCommand`）：注册需要读取配置和日志的子命令（如 `migrate` 数据库迁移、数据导入、后台脚本等，执行前框架自动加载配置并初始化 Logger/Runtime）；
    - **`flag.AddCommandWithBaseExcept(cmd, "gen", "g")`**：**支持参数排除**。默认执行 `InitBase`，但当命令行传入了特定标志（如 `-g/--gen`）时，框架自动跳过 `InitBase` 纯离线秒开；
    - **`goxf.InitBase(opts...)`**：全局函数，支持在任何子命令内部根据代码分支**动态按需手动初始化**基础环境；
    - **`flag.AddWrappedCommand` / `flag.AddWrappedCommandWithBase`**：支持以包装对象 `*flag.Command` 链式注册。
- **全球化 i18n (`i18n`)**：提供标准且灵活的多语言本地化翻译管理映射器。
- **错误码收敛字典 (`ecode`)**：构建带有业务语素解析的全项目定制化 `common_ecode` 表以及灵活透传框架，支持无损附带错误链定位（Stacktrace）。
- **便捷开发者工具 (`util`)**：包含格式化 `xfmt` 输出等内部精简直观的方法增强日常开发可读性。

---

## 7. 致谢 (Acknowledgments)

本项目在架构设计与部分核心功能实现过程中，重点参考和借鉴了优秀的开源项目 [douyu/jupiter](https://github.com/douyu/jupiter)。特此向 Jupiter 团队及贡献者致以诚挚的感谢！

---

> **一句话总结**：`goxf` 通过组合现有成熟中间件与优越的生命周期守护模块，将分散的开发成本整合到一处，极大加速了业务模块迭代时的地基验证周期，是一套可靠的产业级工程化基座。